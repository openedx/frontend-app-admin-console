import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  'loading.page.screenreader.text': {
    id: 'loading.page.screenreader.text',
    defaultMessage: 'Loading',
    description: 'Announced by screen readers while a full-page loading spinner is shown.',
  },
});

export default messages;
